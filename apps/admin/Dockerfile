# syntax=docker/dockerfile:1
# Image du back-office HYMEA (Next.js, basePath /admin, noindex).
#
# ⚠️ TEMPLATE (jalon M0) — l'admin n'est pas encore bootstrappé (Next.js arrive
# en #32). Ce Dockerfile sera finalisé à ce moment-là (build Next en sortie
# `standalone`). Build context = racine du monorepo.
#
# Squelette cible identique à apps/web/Dockerfile (filtre @hymea/admin,
# basePath /admin, EXPOSE 3000 / port dédié).
