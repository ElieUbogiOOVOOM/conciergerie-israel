/** Classes partagées des champs de formulaire du back-office (charte claire). */
export const fieldClass =
  "rounded-sm border border-sable bg-ivoire px-2.5 py-1.5 font-ui text-sm text-encre " +
  "focus-visible:outline-2 focus-visible:outline-or-profond focus-visible:outline-offset-2 " +
  "disabled:opacity-50";

/** Classes du libellé court au-dessus d'un champ. */
export const labelClass = "font-ui text-xs font-medium text-encre-doux";
